/**
 * Breakpoints matching tailwind default breakpoints.
 * If tailwind breakpoints are overridden, these should be too.
 */
const breakpoints = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  xl2: 1536,
};

export default breakpoints;
